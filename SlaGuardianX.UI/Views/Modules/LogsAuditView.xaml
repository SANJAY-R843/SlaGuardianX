<UserControl x:Class="SlaGuardianX.UI.Views.Modules.LogsAuditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel>
            <TextBlock Text="Logs &amp; Audit Trail" Style="{StaticResource PageTitle}"/>
            <TextBlock Text="System event logs and audit records" Style="{StaticResource PageSubtitle}" Margin="0,0,0,16"/>
            <Border Style="{StaticResource Card}" Margin="0,0,0,12" Padding="12">
                <DockPanel>
                    <TextBlock Text="{Binding LogStatus}" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                        <Button Content="Refresh" Command="{Binding RefreshLogsCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,6,0"/>
                        <Button Content="Export Logs" Command="{Binding ExportLogsCommand}" Style="{StaticResource SecondaryButton}"/>
                    </StackPanel>
                </DockPanel>
            </Border>
            <UniformGrid Columns="4" Margin="0,0,0,12">
                <Border Style="{StaticResource Card}" Margin="0,0,6,0" Padding="14">
                    <StackPanel><TextBlock Text="METRIC RECORDS" Style="{StaticResource CardLabel}"/>
                        <TextBlock Text="{Binding MetricCount}" Style="{StaticResource CardValue}" Foreground="{StaticResource AccentCyanBrush}"/></StackPanel>
                </Border>
                <Border Style="{StaticResource Card}" Margin="3,0" Padding="14">
                    <StackPanel><TextBlock Text="SLA RECORDS" Style="{StaticResource CardLabel}"/>
                        <TextBlock Text="{Binding SlaCount}" Style="{StaticResource CardValue}" Foreground="{StaticResource AccentPurpleBrush}"/></StackPanel>
                </Border>
                <Border Style="{StaticResource Card}" Margin="3,0" Padding="14">
                    <StackPanel><TextBlock Text="TOTAL ENTRIES" Style="{StaticResource CardLabel}"/>
                        <TextBlock Text="{Binding TotalLogEntries}" Style="{StaticResource CardValue}" Foreground="#00E676"/></StackPanel>
                </Border>
                <Border Style="{StaticResource Card}" Margin="6,0,0,0" Padding="14">
                    <StackPanel><TextBlock Text="LAST REFRESH" Style="{StaticResource CardLabel}"/>
                        <TextBlock Text="{Binding LastRefreshTime}" Style="{StaticResource CardValue}" Foreground="#FF9800" FontSize="14"/></StackPanel>
                </Border>
            </UniformGrid>
            <Border Style="{StaticResource Card}" Padding="14">
                <StackPanel>
                    <TextBlock Text="Recent Log Entries" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <ListBox ItemsSource="{Binding LogEntries}" Background="Transparent" BorderThickness="0" MaxHeight="400"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,1"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border Background="#10FFFFFF" CornerRadius="3" Padding="10,6">
                                                <TextBlock Text="{Binding}" Foreground="{StaticResource TextMutedBrush}" FontSize="11" FontFamily="Consolas" TextWrapping="Wrap"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
